<h1>New field available for &lt;<%= link_to @parser.title, parser_path(@parser) %>&gt; parser</h1>

<%= render 'form' %>

<%= link_to 'Available fields list', parser_path(@parser), :class=>"crud_button btn_list" %>
